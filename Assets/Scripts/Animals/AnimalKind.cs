namespace AnadoluCiftligi.Animals
{
    /// <summary>
    /// Distinguishes producer subtypes. Currently only used for UI/audio
    /// differentiation; gameplay rules are identical across kinds.
    /// </summary>
    public enum AnimalKind
    {
        Animal = 0,
        Tree = 1
    }
}
